export type FunctionData = {
    color: string;
    expression: string[];
    pathArray: string[][];
  };

 export type ViewBox = {
    x: number;
    y: number;
    width: number;
    height: number;
  };